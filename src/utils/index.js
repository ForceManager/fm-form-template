import formatEntityList from './formatEntityList';
import formatInitData from './formatInitData';
import generateForm from './generateForm';
import validateFields from './validateFields';
import beforeChangePage from './beforeChangePage';
import beforeFinish from './beforeFinish';

const utils = {
  formatEntityList,
  formatInitData,
  generateForm,
  validateFields,
  beforeChangePage,
  beforeFinish,
};

export default utils;
